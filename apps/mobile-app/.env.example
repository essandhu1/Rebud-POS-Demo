# Copy to `.env` in this folder. Restart Expo after changes.
# iOS Simulator / Android emulator on same machine:
EXPO_PUBLIC_API_BASE_URL=http://localhost:4000

# Physical device (Expo Go): use your computer's LAN IP, not localhost:
# EXPO_PUBLIC_API_BASE_URL=http://192.168.1.50:4000
